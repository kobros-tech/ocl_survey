defaults:
  - nc_scenario.yaml

dataset_name: "mnist"
dataset_root: ${hydra:runtime.cwd}/data

factory_args:
  benchmark_name: split_mnist
  n_experiences: 5
  val_size: 0.05
  use_transforms: true
  fixed_class_order: null
